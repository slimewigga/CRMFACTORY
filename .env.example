# Секрет для JWT-токенов (обязательно смени в продакшене!)
JWT_SECRET=придумай-длинный-секретный-ключ-минимум-32-символа

# Порт (облако задаёт само)
PORT=3000

# Путь к базе данных
# Локально: data/crm.db (по умолчанию)
# Render с диском: /var/data/crm.db
DB_PATH=data/crm.db
